interface:
  display_name: "Build Runners"
  short_description: "Build timing and XNNPACK trace runners"
  default_prompt: "Use $build-runners to build and validate SME2-on/off timing and XNNPACK trace runners."
policy:
  allow_implicit_invocation: true
